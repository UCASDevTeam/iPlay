<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="colorPrimary">@color/theme_primary</item>
        <item name="colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="colorAccent">@color/theme_accent_2</item>
    </style>
    <style name="AppTheme.ActionBarStyle.Details" parent="android:Widget.ActionBar">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:titleTextStyle">@style/AppTheme.ActionBarStyle.Details.Title</item>
    </style>
    <style name="AppTheme.Details" parent="android:Theme.Holo">
        <item name="android:actionBarStyle">@style/AppTheme.ActionBarStyle.Details</item>
    </style>
    <style name="AppTheme.ActionBarStyle.Details.Title" parent="android:TextAppearance.Holo.Medium">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">25dp</item>
    </style>
</resources>
